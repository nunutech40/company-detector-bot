[Unit]
Description=Manually requeue a limited Company Detector AI-failure backlog batch
After=network-online.target company-register-worker.service

[Service]
Type=oneshot
WorkingDirectory=%h/.openclaw/webhook
EnvironmentFile=%h/.openclaw/gateway.systemd.env
ExecStart=/usr/bin/node worker.js replay-provider-failures --all --limit 5
